import React from 'react';

const initialState = {
  user: {},
};

const AuthReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AuthReducer;
